// Convenience re-export of the auto-generated Supabase schema types.
// Keep app code importing from `@/lib/database.types` so a future migration
// (e.g. Prisma-generated types) only has to change one file.
export type { Database, Tables, TablesInsert, TablesUpdate } from "@/integrations/supabase/types";
